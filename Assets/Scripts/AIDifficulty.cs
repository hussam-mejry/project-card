public enum AIDifficulty
{
    Easy,       // Makes simple decisions, only considers card values
    Medium,     // Uses basic strategy, considers some special cards
    Hard        // Uses advanced strategy, remembers cards, uses all special abilities
}